{
  "name": "step-todo-card",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/step-todo-card.js --bundle --minify --format=esm --outfile=dist/step-todo-card.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "lit": "^3.1.0"
  }
}
